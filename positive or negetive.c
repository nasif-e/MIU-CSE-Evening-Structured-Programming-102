#include <stdio.h>

int main()
{
    int number;

    printf("Enter a number: \n");
    scanf("%d", &number);
    if
        (number > 0)
        printf("number is a positive number=%d \n", number);
    else if
        (number < 0)
        printf("this is a negative number=%d \n", number);

}
